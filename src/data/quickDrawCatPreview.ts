/**
 * Quick, Draw! preview data — kept for `npm run prepare:quickdraw-cats`.
 * Not displayed in the main Cat Memory Book UI.
 */
import type { QuickDrawCatExample } from '../types';

/** Populated by scripts/prepareQuickDrawCats.js — empty until script runs. */
export const quickDrawCatPreview: QuickDrawCatExample[] = [];
